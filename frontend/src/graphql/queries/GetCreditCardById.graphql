query GetCreditCardById($id: Int!) {
  creditcardById(id: $id) {
    CreditCardID
    CreditCardGroupID
    CardName
    Surcharge
    Installments
    IsActive
    GroupData {
      GroupName
    }
  }
}
