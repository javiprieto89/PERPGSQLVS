mutation CreateUserRecord($input: UserCreate!) {
            createUserRecord(data: $input) {
                UserID
                Nickname
                FullName
                IsActive
            }
        }
