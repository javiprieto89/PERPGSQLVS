mutation Login($input: LoginInput!) {
  login(input: $input) {
    success
    message
    token
    user {
      UserID
      Nickname
      FullName
      IsActive
      UserPermissions {
        UserID
        CompanyID
        CompanyName
        BranchID
        BranchName
        RoleID
        RoleName
      }
    }
  }
}
