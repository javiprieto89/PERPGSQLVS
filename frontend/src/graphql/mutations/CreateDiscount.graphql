mutation CreateDiscount($input: DiscountsCreate!) {
            createDiscount(data: $input) {
                DiscountID
                DiscountName
                Percentage
            }
        }
